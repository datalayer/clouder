# 🧱 Terraform CLI

You need [Terraform](https://developer.hashicorp.com/terraform/install) to provision cloud infrastructure for the AWS Kubeadm deployment path.

Install Terraform from the official documentation and verify:

```bash
terraform version
```

Terraform is required for the steps in [Deploy with Terraform](/deployments/terraform/).

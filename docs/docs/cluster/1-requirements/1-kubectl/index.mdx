# ☸️ Kubectl CLI

You need [Kubectl](https://github.com/kubernetes/kubectl) to access the Kubernetes API.

Follow the official documented steps to install Kubectl.

Optionally, we provide a onliner with the [Clouder CLI](/cluster/requirements/clouder/).

```bash
# Install Kubectl with Clouder.
clouder kubectl install
```

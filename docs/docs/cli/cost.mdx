---
title: clouder cost
sidebar_position: 5
---

# clouder cost

Pricing and node SKU cost helpers.

## Commands

### `clouder cost list`

List known node types for a cloud and region, including hourly prices when available.

```bash
clouder cost list [OPTIONS]
```

| Option | Default | Description |
|--------|---------|-------------|
| `--cloud` | `azure` | Cloud provider (`azure` or `aws`) |
| `--region` | `westeurope` | Cloud region (reserved for future pricing backends) |

**Examples:**

```bash
clouder cost list
clouder cost list --cloud aws --region us-east-1
```

# ☸️ Helm CLI

You need [Helm](https://github.com/helm/helm) to deploy the Helm Charts.

Follow the official documented steps to install Helm.

Optionally, we provide a onliner with the [Clouder CLI](/platform/kubernetes/requirements/cli/clouder).

```bash
# Install Helm with Clouder.
clouder helm install
```
